<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="db.properties">
    </properties>
    <!-- 设置开始的加载时 一些设置 -->
    <settings>
        <!-- 日志 -->
        <!-- <setting name="logImpl" value="STDOUT_LOGGING"/> -->
        <setting name="logImpl" value="LOG4J"/>
        <!-- 打开延迟加载的开关 -->
        <setting name="lazyLoadingEnabled" value="true"/>
        <!-- 将积极加载改为消极加载，即延迟加载 -->
        <setting name="aggressiveLazyLoading" value="false"/>
        <!-- 缓存 -->
        <setting name="cacheEnabled" value="true"/>
    </settings>

    <!--可以在mapper.xml使用别名简写  但不推荐 因为点不进去不好验证对错-->
    <typeAliases>
        <!-- 员工Model的别名 -->
        <typeAlias type="com.java.staff.model.StaffModel" alias="staffModel"/>
        <!-- 登录userModel的别名-->
        <typeAlias type="com.java.user.model.UserModel" alias="userModel"/>
        <!-- 部门类department的别名-->
        <typeAlias type="com.java.department.model.DepartmentModel" alias="departmentModel"/>
    </typeAliases>
</configuration>